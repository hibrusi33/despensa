# ===== INSTRUCCIONES PARA WINDOWS =====
# 1. Actualizar pip: python -m pip install --upgrade pip
# 2. Instalar PyTorch primero (CPU): pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
# 3. Instalar este archivo: pip install -r requirements-windows.txt

# FastAPI y servidor (versiones flexibles para mejor compatibilidad)
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6

# Base de datos - MongoDB
motor>=3.3.0
beanie>=1.24.0
pymongo>=4.6.0

# RAG y LLM - Versiones flexibles
langchain>=0.1.0
langchain-community>=0.0.16
chromadb>=0.4.22
sentence-transformers>=2.3.0

# Utilidades (versiones flexibles para wheels pre-compilados)
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Cliente Ollama
ollama>=0.1.6

# Nota: PyTorch y sus dependencias (numpy, etc.) deben instalarse PRIMERO usando:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
